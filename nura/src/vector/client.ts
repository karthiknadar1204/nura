// Pinecone client.
// One index, four namespaces: wheaton | naperville | chicago | evanston
// Metadata fields (pre-declared at index creation — required for filtering):
//   municipality, county, district_codes[], topic_tags[], section_id, permit_type

// TODO: implement — install @pinecone-database/pinecone and configure
export {}
