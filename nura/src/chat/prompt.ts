// System prompt builder.
// Reads the layer registry from Postgres at startup and templates the
// data inventory section dynamically so the LLM always knows what's loaded.

// TODO: implement
export {}
