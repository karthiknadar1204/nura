// LLM agent conversation loop.
// Manages the multi-turn tool calling cycle: send message → receive tool calls
// → execute via executor.ts → feed results back → stream final response.

// TODO: implement
export {}
